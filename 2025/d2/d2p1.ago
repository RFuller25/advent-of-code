answera := 0
fes := "i".apertu().contentes
linerum := finderum(fes, ",")

des substringes(xes, aa, ba) {
    res := ""
    pro ia in aa..ba {
        res = res + xes[ia]
    }
    redeo res
}

des checkam(ja) {
    res := jes
    lengtha := rerum.a()
    si (lengtha % II == 1) {
        redeo falsus
    }
    remendia := lengtha / II
    acompares := substringes(res, 0, remendia - I)
    bcompares := substringes(res, remendia, lengtha - I)

    si acompares == bcompares {
        redeo verum
    }
    redeo falsus
}

pro xes in linerum {
    yerum := finderum(xes, "-")
    aa := yerum[0].a() - I
    ba := yerum[1].a()

    pro ia in aa..ba {
        si checkam(ia) {
            dici(ies)
            answera = answera + ia
        }
    }
}

dici(answeres)